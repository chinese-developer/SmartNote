package com.smarternote.themes.feature.sport

import com.smarternote.themes.R
import com.smarternote.themes.ThemeConfig

object FeatureSportThemeConfig {
    val themeConfig = ThemeConfig(
        textColor = R.color.feature_sport_text_color_primary
        // 设置其他属性
    )
}