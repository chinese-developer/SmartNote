package com.smarternote.core.di.components

import android.content.Context
import com.smarternote.core.di.BaseAppComponent

class DatabaseComponent(private val context: Context) : BaseAppComponent() {
    override fun init() {
        // 初始化数据库库
    }
}
